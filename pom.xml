<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>vn.uth</groupId>
	<artifactId>CareerCompass</artifactId>
	<packaging>jar</packaging>
	<version>0.0.1-SNAPSHOT</version>
	<name>CareerCompass</name>
	<description>CareerCompass</description>
	<url>https://example.com/CareerCompass</url>
	<licenses>
		<license>
			<name>Apache-2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.html</url>
		</license>
	</licenses>
	<developers>
		<developer>
			<id>developer</id>
			<name>Developer</name>
		</developer>
	</developers>
	<scm>
		<connection>scm:git:git://example.com/CareerCompass.git</connection>
		<developerConnection>scm:git:ssh://example.com/CareerCompass.git</developerConnection>
		<tag>HEAD</tag>
		<url>https://example.com/CareerCompass</url>
	</scm>
	<properties>
		<java.version>21</java.version>

		<!-- Giá trị mặc định cho argLine. JaCoCo prepare-agent sẽ ghi đè bằng cờ
		     Java agent của nó. Khai sẵn ở đây để khi bỏ qua JaCoCo (-Djacoco.skip)
		     thì @{argLine} trong cấu hình Surefire vẫn phân giải được. -->
		<argLine></argLine>

		<!-- ===== Phân tích chất lượng mã nguồn: SonarQube Cloud ===== -->
		<!-- Định danh dự án trên sonarcloud.io. projectKey do SonarCloud tự sinh
		     theo dạng <organization>_<repo>, không được tự đặt lại. -->
		<sonar.organization>phatdayne2005</sonar.organization>
		<sonar.projectKey>phatdayne2005_careercompass</sonar.projectKey>
		<sonar.host.url>https://sonarcloud.io</sonar.host.url>

		<!-- SonarQube KHÔNG tự chạy test và KHÔNG tự đo coverage. Nó chỉ đọc lại
		     báo cáo XML do JaCoCo sinh ra. Thiếu dòng này thì dashboard báo 0.0%. -->
		<sonar.coverage.jacoco.xmlReportPaths>target/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>

		<!-- Loại khỏi phép TÍNH COVERAGE (vẫn được quét tìm lỗi) các lớp không
		     chứa logic nghiệp vụ: DTO, entity, lớp cấu hình, lớp main.
		     Viết unit test cho getter/setter chỉ làm đẹp con số, không bắt được lỗi. -->
		<sonar.coverage.exclusions>
			**/dto/**,
			**/entity/**,
			**/config/**,
			**/*Application.java
		</sonar.coverage.exclusions>

		<!-- Loại HẲN khỏi phân tích: tài nguyên tĩnh và template không phải mã Java. -->
		<sonar.exclusions>
			**/target/**,
			**/static/**,
			**/*.min.js
		</sonar.exclusions>

		<!-- Ghim phiên bản để mọi máy và CI chạy ra cùng kết quả.
		     Spring Boot 4.1 không còn quản lý sẵn jacoco.version nên phải tự khai. -->
		<jacoco.version>0.8.13</jacoco.version>
		<sonar-maven-plugin.version>5.7.0.6970</sonar-maven-plugin.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<!-- OAuth2 Client: cho phép "Login with Google" (oauth2Login). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>

		<dependency> <groupId>org.jsoup</groupId> <artifactId>jsoup</artifactId> <version>1.17.2</version> </dependency>

		<!-- Render markdown do AI trả về (**đậm**, danh sách...) thành HTML để hiển thị đúng. -->
		<dependency> <groupId>org.commonmark</groupId> <artifactId>commonmark</artifactId> <version>0.22.0</version> </dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Surefire tự nạp junit-platform-launcher lúc chạy nhưng KHÔNG đưa vào
		     classpath biên dịch. Khai tường minh để TestCaseLogger (in tên từng
		     test case ra console) biên dịch được. Phiên bản do junit-bom quản lý. -->
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Spring Boot 4.x tách các test slice MVC thành starter riêng. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<!-- OpenPDF: xuất báo cáo Skill Gap ra PDF (FR3.3) -->
		<dependency>
			<groupId>com.github.librepdf</groupId>
			<artifactId>openpdf</artifactId>
			<version>1.3.43</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<annotationProcessorPath>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</annotationProcessorPath>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!-- ===================================================================
			     JaCoCo: đo độ bao phủ mã nguồn của bộ unit test.
			     - prepare-agent: gắn Java agent vào JVM chạy test để ghi lại từng
			       dòng lệnh nào được thực thi (ghi ra target/jacoco.exec).
			     - report: chuyển file nhị phân đó thành HTML (cho người đọc) và
			       XML (cho SonarQube đọc).
			     Lưu ý: Spring Boot 4.1 không còn quản lý sẵn jacoco.version như bản
			     3.x nên phải tự khai phiên bản ở khối <properties>.
			     =================================================================== -->
			<!-- ===================================================================
			     Surefire: ghi TÊN HIỂN THỊ của test vào báo cáo XML.
			     Mặc định XML chỉ ghi "method(int, int, String)[1]" nên không đọc được
			     từng test case là gì. Bật usePhrasedTestCaseMethodName để nó ghi đúng
			     nhãn @ParameterizedTest(name = "..."), ví dụ:
			         TC1 · password = min · (fullName=50, email=75, password=6)
			     Nhờ vậy chiếu báo cáo test cho người khác xem mà không cần mở code.
			     =================================================================== -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- Buộc JVM chạy test in ra UTF-8, nếu không tiếng Việt trong tên
					     test case sẽ thành dấu hỏi trên console Windows.
					     @{argLine} giữ lại cờ Java agent mà JaCoCo chèn vào. -->
					<argLine>@{argLine} -Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8</argLine>
					<statelessTestsetReporter
						implementation="org.apache.maven.plugin.surefire.extensions.junit5.JUnit5Xml30StatelessReporter">
						<usePhrasedFileName>false</usePhrasedFileName>
						<usePhrasedTestSuiteClassName>true</usePhrasedTestSuiteClassName>
						<usePhrasedTestCaseClassName>true</usePhrasedTestCaseClassName>
						<usePhrasedTestCaseMethodName>true</usePhrasedTestCaseMethodName>
					</statelessTestsetReporter>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<!-- Chạy sau khi test xong, trước khi đóng gói. -->
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-check</id>
						<!-- verify được workflow CI gọi sau khi test và tạo report. -->
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.65</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- SonarScanner for Maven: chạy phân tích ngay tại máy build rồi
			     gửi báo cáo lên SonarQube Cloud. Khai báo ở đây để lệnh rút gọn
			     "mvn sonar:sonar" luôn dùng đúng phiên bản đã ghim. -->
			<plugin>
				<groupId>org.sonarsource.scanner.maven</groupId>
				<artifactId>sonar-maven-plugin</artifactId>
				<version>${sonar-maven-plugin.version}</version>
			</plugin>
		</plugins>
	</build>

</project>
